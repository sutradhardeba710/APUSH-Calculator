---
interface Props {
  id: 'mcq' | 'saq' | 'dbq' | 'leq';
  label: string;
  shortName: string;
  max: number;
  weightPercent: number;
  defaultValue?: number;
  description: string;
  rubricLink?: string;
}

const {
  id,
  label,
  shortName,
  max,
  weightPercent,
  defaultValue = 0,
  description,
  rubricLink,
} = Astro.props;

const inputId = `score-input-${id}`;
const sliderId = `score-slider-${id}`;
const errorId = `score-error-${id}`;
const hintId = `score-hint-${id}`;
---

<div class="score-input-card p-4 sm:p-5 rounded-xl bg-white dark:bg-[#131b2e] border border-neutral-200 dark:border-[#243354] hover:border-neutral-300 dark:hover:border-[#334a75] transition-all">
  <!-- Header row -->
  <div class="flex items-start justify-between gap-2 mb-2">
    <div>
      <div class="flex items-center gap-2">
        <label for={inputId} class="font-heading font-bold text-base text-neutral-900 dark:text-white cursor-pointer">
          {label}
        </label>
        <span class="inline-flex items-center px-2 py-0.5 rounded text-[10px] font-semibold bg-blue-50 dark:bg-blue-950/60 text-blue-700 dark:text-blue-300 border border-blue-100 dark:border-blue-800/60">
          {weightPercent}% of Exam
        </span>
      </div>
      <p id={hintId} class="text-xs text-neutral-500 dark:text-neutral-400 mt-0.5">
        {description}
        {rubricLink && (
          <a href={rubricLink} class="text-blue-600 dark:text-blue-400 hover:text-blue-800 dark:hover:text-blue-300 underline ml-1">
            Rubric guide →
          </a>
        )}
      </p>
    </div>

    <!-- Stepper & Input group -->
    <div class="flex items-center gap-1 shrink-0">
      <button
        type="button"
        class="step-btn step-down w-7 h-7 rounded-md bg-neutral-100 dark:bg-neutral-800 hover:bg-neutral-200 dark:hover:bg-neutral-700 active:bg-neutral-300 text-neutral-700 dark:text-neutral-200 font-bold flex items-center justify-center transition-colors focus-visible:outline-2 focus-visible:outline-blue-600 text-xs"
        data-target={inputId}
        aria-label={`Decrease ${shortName} score`}
      >
        −
      </button>

      <div class="relative flex items-center">
        <input
          type="number"
          id={inputId}
          name={id}
          min="0"
          max={max}
          value={defaultValue}
          inputmode="numeric"
          pattern="[0-9]*"
          class="score-number-input w-16 h-9 text-center font-heading font-bold text-base text-neutral-900 dark:text-white bg-neutral-50 dark:bg-[#1a233a] border border-neutral-300 dark:border-[#2e3f66] rounded-lg focus:bg-white dark:focus:bg-[#131b2e] focus:border-blue-600 dark:focus:border-blue-500 focus:ring-2 focus:ring-blue-100 dark:focus:ring-blue-950 transition-all outline-none"
          aria-describedby={`${hintId} ${errorId}`}
          aria-invalid="false"
        />
      </div>

      <button
        type="button"
        class="step-btn step-up w-7 h-7 rounded-md bg-neutral-100 dark:bg-neutral-800 hover:bg-neutral-200 dark:hover:bg-neutral-700 active:bg-neutral-300 text-neutral-700 dark:text-neutral-200 font-bold flex items-center justify-center transition-colors focus-visible:outline-2 focus-visible:outline-blue-600 text-xs"
        data-target={inputId}
        aria-label={`Increase ${shortName} score`}
      >
        +
      </button>
    </div>
  </div>

  <!-- Range Slider & Max label -->
  <div class="mt-3 pt-2 border-t border-neutral-100 dark:border-neutral-800 flex items-center gap-3">
    <input
      type="range"
      id={sliderId}
      min="0"
      max={max}
      value={defaultValue}
      class="score-range-slider flex-1 h-2 bg-neutral-200 dark:bg-[#1e293b] rounded-lg appearance-none cursor-pointer accent-blue-600"
      aria-label={`${shortName} score slider`}
    />
    <span class="text-xs font-semibold text-neutral-500 dark:text-neutral-400 w-12 text-right">
      <span class="current-val text-neutral-900 dark:text-white font-bold">{defaultValue}</span> / {max}
    </span>
  </div>

  <!-- Inline Error Notification -->
  <p id={errorId} class="text-xs text-rose-600 font-medium mt-1.5 hidden" role="alert"></p>
</div>
